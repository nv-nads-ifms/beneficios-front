import React from 'react';

// const objectContext = React.createContext({formId, setFormId, dataControl, setDataControl, open, setOpen});
const formContext = React.createContext({});

export { formContext }; 